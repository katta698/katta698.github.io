<svg viewBox="0 0 860 520" style="width:100%;max-width:860px;" role="img" xmlns="http://www.w3.org/2000/svg" aria-label="Diagram of how an AWS commitment discount is assigned each hour and the three sharing modes that scope it. The application order runs top to bottom: EC2 Reserved Instances first, then EC2 Instance Savings Plans, then Compute Savings Plans because those have broader applicability, and any remaining usage is charged at On-Demand rates. Within Savings Plans the commitment is applied to the highest savings percentage first, with ties broken by the lowest Savings Plans rate, and each hour's commitment can only be used within that hour and cannot be carried over. Across an organisation the owner account is satisfied first and then the benefit flows outward according to the sharing mode: organization-wide sharing spreads the remaining benefit across all accounts, prioritized group sharing serves a defined group before the rest of the organisation, and restricted group sharing shares only within the defined group even when capacity goes unused. A panel at the bottom records that the final monthly bill is calculated from the sharing preferences as they stood at 23:59:59 UTC on the last day of the month, so a change made late in the month rewrites the allocation for every hour since the first.">
  <title>A commitment is a discount schedule applied in a fixed order, not an asset held by an account</title>
  <rect width="860" height="520" fill="#F8F7F5"/>

  <text x="44" y="36" font-family="Georgia, serif" font-size="15" font-weight="bold" fill="#1D2322">The order is fixed. The destination is not.</text>
  <text x="44" y="57" font-family="Georgia, serif" font-size="11" fill="#6B6560">Recomputed every hour, owner first, then whichever account saves most &#8212; and unused commitment does not carry over.</text>

  <!-- application order -->
  <text x="44" y="88" font-family="Georgia, serif" font-size="12" font-weight="bold" fill="#1D2322">1 &#183; What is applied, in what order</text>

  <rect x="44" y="100" width="376" height="30" rx="3" fill="#5B7B9A" opacity="0.22"/>
  <text x="58" y="120" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#2E4256">EC2 Reserved Instances</text>
  <text x="300" y="120" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#6B6560">applied first</text>

  <rect x="44" y="136" width="376" height="30" rx="3" fill="#5B7B9A" opacity="0.16"/>
  <text x="58" y="156" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#2E4256">EC2 Instance Savings Plans</text>
  <text x="300" y="156" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#6B6560">narrower, so first</text>

  <rect x="44" y="172" width="376" height="30" rx="3" fill="#5B7B9A" opacity="0.10"/>
  <text x="58" y="192" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#2E4256">Compute Savings Plans</text>
  <text x="300" y="192" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#6B6560">broader, so last</text>

  <rect x="44" y="208" width="376" height="30" rx="3" fill="#B5654A" opacity="0.12"/>
  <text x="58" y="228" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#8F4A32">Everything left &#8212; On-Demand</text>

  <text x="44" y="260" font-family="Georgia, serif" font-size="10" fill="#3D2E20">Within Savings Plans: highest savings percentage first,</text>
  <text x="44" y="275" font-family="Georgia, serif" font-size="10" fill="#3D2E20">ties broken by the lowest Savings Plans rate.</text>
  <text x="44" y="295" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#8F4A32">Each hour&#8217;s commitment is usable only in that hour.</text>

  <!-- sharing modes -->
  <text x="440" y="88" font-family="Georgia, serif" font-size="12" font-weight="bold" fill="#1D2322">2 &#183; Who is eligible to receive it</text>

  <rect x="440" y="100" width="376" height="62" rx="3" fill="#8A9A5B" opacity="0.13"/>
  <rect x="440" y="100" width="376" height="62" rx="3" fill="none" stroke="#8A9A5B" stroke-width="1.5"/>
  <text x="454" y="120" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#5F6E38">Organization-wide</text>
  <text x="454" y="138" font-family="Georgia, serif" font-size="9.5" fill="#3D4A22">Owner &#8594; any account in the organisation.</text>
  <text x="454" y="153" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#6B6560">Best utilisation, least predictable beneficiary.</text>

  <rect x="440" y="170" width="376" height="62" rx="3" fill="#C4A484" opacity="0.18"/>
  <rect x="440" y="170" width="376" height="62" rx="3" fill="none" stroke="#C4A484" stroke-width="1.5"/>
  <text x="454" y="190" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#8A6A44">Prioritized group</text>
  <text x="454" y="208" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">Owner &#8594; defined group &#8594; rest of the organisation.</text>
  <text x="454" y="223" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#6B6560">First claim without stranding the remainder.</text>

  <rect x="440" y="240" width="376" height="62" rx="3" fill="#B5654A" opacity="0.11"/>
  <rect x="440" y="240" width="376" height="62" rx="3" fill="none" stroke="#B5654A" stroke-width="1.5"/>
  <text x="454" y="260" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#8F4A32">Restricted group</text>
  <text x="454" y="278" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">Owner &#8594; defined group, and nowhere else.</text>
  <text x="454" y="293" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#8F4A32">&#8220;Even with unused capacity.&#8221; Predictable, and it strands.</text>

  <!-- month-end -->
  <rect x="44" y="324" width="772" height="74" rx="3" fill="#B5654A" opacity="0.09"/>
  <rect x="44" y="324" width="772" height="74" rx="3" fill="none" stroke="#B5654A" stroke-width="1.8"/>
  <text x="60" y="348" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#8F4A32">And the whole month is settled by one setting</text>
  <text x="60" y="369" font-family="Georgia, serif" font-size="10" fill="#3D2E20">&#8220;Each estimated bill is computed by using the last set of preferences. The final bill for the month is calculated based on the</text>
  <text x="60" y="384" font-family="Georgia, serif" font-size="10" fill="#3D2E20">preferences set at 23:59:59 UTC time on the last day of the month.&#8221; A change on the 30th rewrites every hour since the 1st.</text>

  <text x="44" y="432" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#1D2322">Three consequences worth writing down</text>
  <text x="44" y="451" font-family="Georgia, serif" font-size="10" fill="#3D2E20">&#8226; The purchase account is the payer of record, not the beneficiary. Expect the benefit to land elsewhere.</text>
  <text x="44" y="466" font-family="Georgia, serif" font-size="10" fill="#3D2E20">&#8226; A mid-month chargeback figure is an estimate. Say so on the report.</text>
  <text x="44" y="481" font-family="Georgia, serif" font-size="10" fill="#3D2E20">&#8226; If the Savings Plans owner account leaves the organisation, the plans stop applying to the consolidated bill.</text>

  <text x="44" y="508" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#6B6560">Both the purchasing and the receiving account must have sharing activated &#8212; switching it off on the purchaser stops its own commitment reaching anyone.</text>
</svg>
