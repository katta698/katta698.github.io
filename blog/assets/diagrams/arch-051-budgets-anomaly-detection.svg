<svg viewBox="0 0 860 520" style="width:100%;max-width:860px;" role="img" xmlns="http://www.w3.org/2000/svg" aria-label="Diagram of the AWS cost control pipeline and where its latency comes from. Usage occurs on the left, then passes through the billing pipeline, where AWS states billing data is updated at least once per day and Cost Explorer data has a delay of up to 24 hours. Two detectors read from the end of that pipeline: AWS Budgets, which is updated up to three times a day with updates typically 8 to 12 hours apart and compares spend against a threshold the customer chose, and Cost Anomaly Detection, which runs approximately three times a day using machine learning models against net unblended cost data and compares spend against the customer's own historical pattern. Because detection reads the bill rather than the resources, AWS states it can take up to 24 hours to detect an anomaly after a usage occurs, which is the floor no configuration lowers. Only one path leads to enforcement: a budget action, which can apply an IAM policy or a service control policy or target EC2 and RDS instances, and which can be set to run automatically or only after manual approval. A panel records two ways the stop fails quietly: an instance stopped by a budget action inside an Auto Scaling group is restarted or replaced unless a second action removes the scaling role's permissions, and from the management account an SCP can be applied to another account but EC2 and RDS instances in another account cannot be targeted.">
  <title>Both detectors read the bill, not the resources</title>
  <rect width="860" height="520" fill="#F8F7F5"/>

  <text x="44" y="36" font-family="Georgia, serif" font-size="15" font-weight="bold" fill="#1D2322">Nothing here is watching your resources</text>
  <text x="44" y="57" font-family="Georgia, serif" font-size="11" fill="#6B6560">Both detectors read the bill, and the bill is assembled on its own schedule &#8212; which sets a floor no threshold setting lowers.</text>

  <!-- pipeline row -->
  <rect x="44" y="84" width="150" height="58" rx="3" fill="#EDE8E2" stroke="#C9C3BC" stroke-width="1"/>
  <text x="58" y="106" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#3D2E20">1 &#183; Usage occurs</text>
  <text x="58" y="124" font-family="Georgia, serif" font-size="9" fill="#6B6560">The resource runs.</text>
  <text x="58" y="137" font-family="Georgia, serif" font-size="9" fill="#6B6560">Nothing watches it.</text>

  <text x="203" y="118" font-family="Georgia, serif" font-size="15" fill="#8A6A44">&#8594;</text>

  <rect x="224" y="84" width="300" height="58" rx="3" fill="#5B7B9A" opacity="0.14"/>
  <rect x="224" y="84" width="300" height="58" rx="3" fill="none" stroke="#5B7B9A" stroke-width="1.5"/>
  <text x="238" y="106" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#3F5A73">2 &#183; The billing pipeline</text>
  <text x="238" y="124" font-family="Georgia, serif" font-size="9" fill="#2E4256">&#8220;updated at least once per day&#8221;</text>
  <text x="238" y="137" font-family="Georgia, serif" font-size="9" fill="#2E4256">Cost Explorer: &#8220;delay of up to 24 hours&#8221;</text>

  <text x="533" y="118" font-family="Georgia, serif" font-size="15" fill="#8A6A44">&#8594;</text>

  <rect x="554" y="84" width="262" height="58" rx="3" fill="#B5654A" opacity="0.13"/>
  <rect x="554" y="84" width="262" height="58" rx="3" fill="none" stroke="#B5654A" stroke-width="1.5"/>
  <text x="568" y="106" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#8F4A32">3 &#183; The floor</text>
  <text x="568" y="124" font-family="Georgia, serif" font-size="9" fill="#3D2E20">&#8220;up to 24 hours to detect an</text>
  <text x="568" y="137" font-family="Georgia, serif" font-size="9" fill="#3D2E20">anomaly after a usage occurs&#8221;</text>

  <!-- two detectors -->
  <rect x="44" y="170" width="376" height="130" rx="3" fill="#5B7B9A" opacity="0.10"/>
  <rect x="44" y="170" width="376" height="130" rx="3" fill="none" stroke="#5B7B9A" stroke-width="1.5"/>
  <text x="58" y="193" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#3F5A73">AWS Budgets</text>
  <text x="58" y="213" font-family="Georgia, serif" font-size="9.5" fill="#2E4256">Updated up to three times a day, typically 8&#8211;12 hours apart.</text>
  <text x="58" y="234" font-family="Georgia, serif" font-size="9.5" font-weight="bold" fill="#2E4256">Compares spend against a threshold you chose.</text>
  <text x="58" y="255" font-family="Georgia, serif" font-size="9.5" fill="#2E4256">Actual alert: once per budget, per budget period.</text>
  <text x="58" y="272" font-family="Georgia, serif" font-size="9.5" fill="#2E4256">Forecast alert: can re-fire, and arrives before accrual.</text>
  <text x="58" y="290" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#8F4A32">Forecasts need roughly 5 weeks of usage data first.</text>

  <rect x="440" y="170" width="376" height="130" rx="3" fill="#8A9A5B" opacity="0.12"/>
  <rect x="440" y="170" width="376" height="130" rx="3" fill="none" stroke="#8A9A5B" stroke-width="1.5"/>
  <text x="454" y="193" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#5F6E38">Cost Anomaly Detection</text>
  <text x="454" y="213" font-family="Georgia, serif" font-size="9.5" fill="#3D4A22">Runs about three times a day on net unblended cost.</text>
  <text x="454" y="234" font-family="Georgia, serif" font-size="9.5" font-weight="bold" fill="#3D4A22">Compares spend against your own history, using ML.</text>
  <text x="454" y="255" font-family="Georgia, serif" font-size="9.5" fill="#3D4A22">Root cause ranked by dollar impact, across four:</text>
  <text x="454" y="272" font-family="Consolas, monospace" font-size="9" fill="#5F6E38">service &#183; account &#183; Region &#183; usage type</text>
  <text x="454" y="290" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#8F4A32">Excludes Marketplace, except Bedrock third-party models.</text>

  <!-- enforcement -->
  <rect x="44" y="328" width="772" height="74" rx="3" fill="#8A9A5B" opacity="0.10"/>
  <rect x="44" y="328" width="772" height="74" rx="3" fill="none" stroke="#8A9A5B" stroke-width="1.8"/>
  <text x="60" y="352" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#5F6E38">Only one path actually stops anything &#8212; a budget action</text>
  <text x="60" y="373" font-family="Georgia, serif" font-size="10" fill="#3D4A22">Apply an IAM policy or an SCP &#183; target EC2 or RDS instances &#183; run automatically, or only after manual approval.</text>
  <text x="60" y="390" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#6B6560">Manual approval adds human latency on top of a detection floor already measured in hours.</text>

  <!-- quiet failures -->
  <rect x="44" y="414" width="772" height="66" rx="3" fill="#B5654A" opacity="0.09"/>
  <rect x="44" y="414" width="772" height="66" rx="3" fill="none" stroke="#B5654A" stroke-width="1.5"/>
  <text x="60" y="437" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#8F4A32">Two ways the stop fails quietly</text>
  <text x="60" y="457" font-family="Georgia, serif" font-size="10" fill="#3D2E20">&#8226; Inside an Auto Scaling group, a stopped instance is restarted or replaced &#8212; unless a second action strips the scaling role.</text>
  <text x="60" y="472" font-family="Georgia, serif" font-size="10" fill="#3D2E20">&#8226; From the management account an SCP reaches another account; EC2 and RDS instances in another account cannot be targeted.</text>

  <text x="44" y="502" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#6B6560">The limit belongs where provisioning happens. These two tell you, as early as the bill can, that something unintended is underway.</text>
</svg>
