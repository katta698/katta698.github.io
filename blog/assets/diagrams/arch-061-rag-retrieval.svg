<svg viewBox="0 0 860 540" style="width:100%;max-width:860px;" role="img" xmlns="http://www.w3.org/2000/svg" aria-label="Diagram: where a retrieval-augmented generation pipeline actually fails on AWS. Ingestion runs left to right: a document is split into chunks, each chunk is converted to an embedding and written to a vector index while a mapping back to the original document is maintained. At query time the question is embedded and compared against that index, the top matches are placed into the prompt, and the model generates an answer. A panel marks the first half of the pipeline as the part that decides what can ever be found, and notes that by the time anyone debugs a wrong answer the chunk boundaries are historical and appear in no request or response. The chunking strategies are compared: default chunking splits content into approximately 300 tokens while honouring sentence boundaries, fixed-size chunking lets you set tokens per chunk and an overlap percentage, no chunking treats each document as a single chunk and removes page numbers from citations, semantic chunking uses a buffer size where a value of one embeds three sentences together and a breakpoint percentile threshold where a higher value produces fewer and larger chunks at additional foundation model cost, and hierarchical chunking retrieves small child chunks but returns their larger parents. A panel records AWS's own statement of the central tension, that small text embeddings are more precise but retrieval aims for comprehensive context, and notes that hierarchical chunking is the only strategy that does not force a choice between the two, at the cost that the number of returned results may be fewer than requested because children are replaced by parents.">
  <title>Everything before the arrow decides what can ever be found</title>
  <rect width="860" height="540" fill="#F8F7F5"/>

  <text x="44" y="36" font-family="Georgia, serif" font-size="15" font-weight="bold" fill="#1D2322">A wrong answer from missing context looks exactly like a weak model</text>
  <text x="44" y="57" font-family="Georgia, serif" font-size="11" fill="#6B6560">There is no error anywhere in the pipeline. The model answered from what it was given.</text>

  <!-- ingestion -->
  <rect x="44" y="84" width="404" height="120" rx="3" fill="#5B7B9A" opacity="0.12"/>
  <rect x="44" y="84" width="404" height="120" rx="3" fill="none" stroke="#5B7B9A" stroke-width="1.6"/>
  <text x="58" y="106" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#3F5A73">Ingestion &#8212; decides what can ever be found</text>

  <rect x="58" y="118" width="108" height="34" rx="3" fill="#5B7B9A" opacity="0.22"/>
  <text x="72" y="139" font-family="Georgia, serif" font-size="9" fill="#2E4256">split into chunks</text>
  <text x="174" y="139" font-family="Georgia, serif" font-size="12" fill="#8A6A44">&#8594;</text>
  <rect x="192" y="118" width="108" height="34" rx="3" fill="#5B7B9A" opacity="0.22"/>
  <text x="206" y="139" font-family="Georgia, serif" font-size="9" fill="#2E4256">embed each chunk</text>
  <text x="308" y="139" font-family="Georgia, serif" font-size="12" fill="#8A6A44">&#8594;</text>
  <rect x="326" y="118" width="108" height="34" rx="3" fill="#5B7B9A" opacity="0.22"/>
  <text x="340" y="139" font-family="Georgia, serif" font-size="9" fill="#2E4256">vector index</text>

  <text x="58" y="172" font-family="Georgia, serif" font-size="9.5" fill="#2E4256">A mapping back to the source document is kept &#8212;</text>
  <text x="58" y="186" font-family="Georgia, serif" font-size="9.5" fill="#2E4256">that mapping is what makes citations possible.</text>

  <text x="458" y="150" font-family="Georgia, serif" font-size="15" fill="#8A6A44">&#8594;</text>

  <!-- query -->
  <rect x="482" y="84" width="334" height="120" rx="3" fill="#EDE8E2"/>
  <rect x="482" y="84" width="334" height="120" rx="3" fill="none" stroke="#C9C3BC" stroke-width="1.3"/>
  <text x="496" y="106" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#3D2E20">Query &#8212; where the attention goes</text>
  <text x="496" y="128" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">embed the question &#8594; nearest neighbours</text>
  <text x="496" y="146" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">&#8594; top matches into the prompt &#8594; generate</text>
  <text x="496" y="172" font-family="Georgia, serif" font-size="9" font-style="italic" fill="#6B6560">Prompt, model, temperature, result count &#8212;</text>
  <text x="496" y="186" font-family="Georgia, serif" font-size="9" font-style="italic" fill="#6B6560">all live, all adjustable, none of them the cause.</text>

  <!-- strategies -->
  <text x="44" y="234" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#5F6E38">The four strategies, and what each trades</text>

  <rect x="44" y="244" width="376" height="42" rx="3" fill="#8A9A5B" opacity="0.13"/>
  <text x="58" y="262" font-family="Consolas, monospace" font-size="9" font-weight="bold" fill="#3D4A22">Default</text>
  <text x="58" y="277" font-family="Georgia, serif" font-size="9.5" fill="#3D4A22">~300 tokens, honouring sentence boundaries. A paragraph.</text>

  <rect x="440" y="244" width="376" height="42" rx="3" fill="#8A9A5B" opacity="0.13"/>
  <text x="454" y="262" font-family="Consolas, monospace" font-size="9" font-weight="bold" fill="#3D4A22">Fixed-size</text>
  <text x="454" y="277" font-family="Georgia, serif" font-size="9.5" fill="#3D4A22">Tokens per chunk + overlap %. Overlap is the cheap fix.</text>

  <rect x="44" y="294" width="376" height="42" rx="3" fill="#C4A484" opacity="0.18"/>
  <text x="58" y="312" font-family="Consolas, monospace" font-size="9" font-weight="bold" fill="#8A6A44">No chunking</text>
  <text x="58" y="327" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">One chunk per document &#8212; and no page number in citations.</text>

  <rect x="440" y="294" width="376" height="42" rx="3" fill="#C4A484" opacity="0.18"/>
  <text x="454" y="312" font-family="Consolas, monospace" font-size="9" font-weight="bold" fill="#8A6A44">Semantic</text>
  <text x="454" y="327" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">Splits on meaning. The only one with a running model bill.</text>

  <!-- the tension -->
  <rect x="44" y="352" width="772" height="94" rx="3" fill="#B5654A" opacity="0.10"/>
  <rect x="44" y="352" width="772" height="94" rx="3" fill="none" stroke="#B5654A" stroke-width="1.8"/>
  <text x="60" y="374" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#8F4A32">The tension, in AWS&#8217;s own sentence</text>
  <text x="60" y="395" font-family="Georgia, serif" font-size="10.5" font-style="italic" fill="#3D2E20">&#8220;Small text embeddings are more precise, but retrieval aims for comprehensive context.&#8221;</text>
  <text x="60" y="417" font-family="Georgia, serif" font-size="10" fill="#3D2E20">Small chunks match sharply and arrive without the surrounding material. Large chunks embed an average and drift.</text>
  <text x="60" y="435" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#8F4A32">Hierarchical is the only strategy that refuses the trade: match on children, answer from parents.</text>

  <text x="44" y="474" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#6B6560">Its tell looks like a bug: ask for five results and receive three, because two children shared a parent. That is correct behaviour.</text>
  <text x="44" y="492" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#5F6E38">Measure retrieval alone before evaluating a prompt: how often was the answer present in what came back? Nothing downstream raises that ceiling.</text>
</svg>
