<svg viewBox="0 0 860 600" style="width:100%;max-width:860px;" role="img" xmlns="http://www.w3.org/2000/svg" aria-label="Diagram: three AWS vector stores arranged on the axis of query frequency rather than price. On the left, Amazon S3 Vectors, described by AWS as ideal for workloads where queries are less frequent, offering subsecond latency for infrequent queries and as low as 100 milliseconds for more frequent ones. It holds up to 2 billion vectors per index and 10,000 indexes per bucket, allows 1 to 4,096 dimensions per vector, and permits 40 kilobytes of metadata per vector of which only 2 kilobytes may be filterable, with all metadata filterable by default. Its writes are strongly consistent so the most recently added data can be read immediately, and it uses the separate s3vectors IAM namespace with S3 Block Public Access always enabled and impossible to disable. In the centre, Amazon OpenSearch Serverless vector search collections, built for high queries per second and low latency, accommodating 16,000 dimensions, supporting full-text search, advanced filtering, aggregations, geospatial and nested queries alongside vector search. NextGen collections scale both indexing and search to zero when idle, create all indexes with 32x compression by default with 1x, 2x, 8x, 16x and 32x available, and carry a read-after-write refresh interval of 10 seconds, while Classic collections refresh at 60 seconds. Radial search is unsupported on NextGen indexes using 32x compression, and needing more than 200 OpenSearch Compute Units requires contacting AWS Support. On the right, Aurora PostgreSQL with the pgvector extension, which requires pgvector version 0.5.0 or higher for HNSW indexing, an ef_construction value of 256 recommended for version 0.6.0 and above, the RDS Data API, and a user managed in Secrets Manager, with the vector dimension written into the table schema so that changing embedding model is a migration. A central panel records that AWS documents a tiering between the first two: a snapshot of an S3 vector index can be exported to OpenSearch Serverless for high queries per second and low latency search, so they are layers rather than competitors. A closing panel records the counterintuitive finding that the cheapest store is the freshest, since S3 Vectors writes are strongly consistent and immediately readable while the search-optimised store lags by 10 seconds, or 60 on Classic.">
  <title>One axis: how often is it asked, not how much does it cost to hold</title>
  <rect width="860" height="600" fill="#F8F7F5"/>

  <text x="44" y="36" font-family="Georgia, serif" font-size="15" font-weight="bold" fill="#1D2322">The price difference is buying a query profile, not a discount</text>
  <text x="44" y="57" font-family="Georgia, serif" font-size="11" fill="#6B6560">Chosen on cost per gigabyte, a vector store can be cheap to hold and wrong to use.</text>

  <!-- axis -->
  <text x="44" y="84" font-family="Georgia, serif" font-size="9.5" font-style="italic" fill="#6B6560">queries are infrequent &#8594;&#8594;&#8594; queries are constant &#8594;&#8594;&#8594; queries must join your rows</text>

  <!-- s3 vectors -->
  <rect x="44" y="96" width="252" height="196" rx="3" fill="#C4A484" opacity="0.18"/>
  <rect x="44" y="96" width="252" height="196" rx="3" fill="none" stroke="#C4A484" stroke-width="1.6"/>
  <text x="58" y="118" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#8A6A44">S3 Vectors</text>
  <text x="58" y="136" font-family="Georgia, serif" font-size="9" font-style="italic" fill="#3D2E20">&#8220;ideal for workloads where queries</text>
  <text x="58" y="149" font-family="Georgia, serif" font-size="9" font-style="italic" fill="#3D2E20">are less frequent&#8221;</text>
  <text x="58" y="168" font-family="Georgia, serif" font-size="9" fill="#3D2E20">subsecond, as low as 100 ms when frequent</text>
  <text x="58" y="184" font-family="Georgia, serif" font-size="9" fill="#3D2E20">2 billion vectors per index</text>
  <text x="58" y="199" font-family="Georgia, serif" font-size="9" fill="#3D2E20">10,000 indexes per bucket</text>
  <text x="58" y="214" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#8A6A44">1 to 4,096 dimensions</text>
  <text x="58" y="230" font-family="Georgia, serif" font-size="9" fill="#3D2E20">40 KB metadata &#8212; but only 2 KB filterable</text>
  <text x="58" y="245" font-family="Georgia, serif" font-size="9" fill="#3D2E20">(and filterable is the default)</text>
  <text x="58" y="263" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#5F6E38">writes strongly consistent &#8212; read immediately</text>
  <text x="58" y="279" font-family="Georgia, serif" font-size="8.5" fill="#6B6560">s3vectors namespace &#183; Block Public Access forced on</text>

  <!-- opensearch -->
  <rect x="306" y="96" width="252" height="196" rx="3" fill="#5B7B9A" opacity="0.14"/>
  <rect x="306" y="96" width="252" height="196" rx="3" fill="none" stroke="#5B7B9A" stroke-width="1.6"/>
  <text x="320" y="118" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#3F5A73">OpenSearch Serverless</text>
  <text x="320" y="136" font-family="Georgia, serif" font-size="9" font-style="italic" fill="#2E4256">built for high QPS and low latency</text>
  <text x="320" y="155" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#3F5A73">16,000 dimensions</text>
  <text x="320" y="171" font-family="Georgia, serif" font-size="9" fill="#2E4256">full-text, filtering, aggregations, geospatial</text>
  <text x="320" y="186" font-family="Georgia, serif" font-size="9" fill="#2E4256">NextGen scales indexing and search to zero</text>
  <text x="320" y="201" font-family="Georgia, serif" font-size="9" fill="#2E4256">32x compression by default (1x&#8211;32x available)</text>
  <text x="320" y="216" font-family="Georgia, serif" font-size="9" fill="#2E4256">radial search unsupported at 32x</text>
  <text x="320" y="231" font-family="Georgia, serif" font-size="9" fill="#2E4256">&gt;200 OCUs needs an AWS Support request</text>
  <text x="320" y="254" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#8F4A32">read-after-write: 10 s NextGen, 60 s Classic</text>
  <text x="320" y="272" font-family="Georgia, serif" font-size="8.5" fill="#6B6560">fast to answer &#8212; and up to a minute behind</text>

  <!-- aurora -->
  <rect x="568" y="96" width="248" height="196" rx="3" fill="#8A9A5B" opacity="0.14"/>
  <rect x="568" y="96" width="248" height="196" rx="3" fill="none" stroke="#8A9A5B" stroke-width="1.6"/>
  <text x="582" y="118" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#3D4A22">Aurora PostgreSQL + pgvector</text>
  <text x="582" y="136" font-family="Georgia, serif" font-size="9" font-style="italic" fill="#3D4A22">vectors beside the rows they describe</text>
  <text x="582" y="155" font-family="Georgia, serif" font-size="9" fill="#3D4A22">pgvector 0.5.0+ for HNSW indexing</text>
  <text x="582" y="170" font-family="Georgia, serif" font-size="9" fill="#3D4A22">ef_construction = 256 on 0.6.0 and higher</text>
  <text x="582" y="185" font-family="Georgia, serif" font-size="9" fill="#3D4A22">needs RDS Data API + Secrets Manager</text>
  <text x="582" y="200" font-family="Georgia, serif" font-size="9" fill="#3D4A22">three indexes: HNSW, GIN text, GIN metadata</text>
  <text x="582" y="219" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#3D4A22">dimension is in the schema: vector(1024)</text>
  <text x="582" y="234" font-family="Georgia, serif" font-size="9" fill="#3D4A22">so changing model is a migration</text>
  <text x="582" y="257" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#5F6E38">the only one where a permission check</text>
  <text x="582" y="272" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#5F6E38">is a join, not a second system</text>

  <!-- tiering -->
  <rect x="44" y="308" width="772" height="74" rx="3" fill="#EDE8E2"/>
  <rect x="44" y="308" width="772" height="74" rx="3" fill="none" stroke="#C9C3BC" stroke-width="1.3"/>
  <text x="60" y="328" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#3D2E20">AWS tiers the first two for you &#8212; they are layers, not competitors</text>
  <text x="60" y="349" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#3D2E20">&#8220;export a snapshot of an S3 vector index to Amazon OpenSearch Serverless for high QPS and low latency vector search&#8221;</text>
  <text x="60" y="369" font-family="Georgia, serif" font-size="10" fill="#3D2E20">Hold the corpus where storage is cheap &#183; promote the subset that serves live queries to where queries are cheap.</text>

  <!-- the inversion -->
  <rect x="44" y="398" width="772" height="98" rx="3" fill="#B5654A" opacity="0.10"/>
  <rect x="44" y="398" width="772" height="98" rx="3" fill="none" stroke="#B5654A" stroke-width="1.8"/>
  <text x="60" y="420" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#8F4A32">The freshness result runs opposite to the price</text>
  <text x="60" y="441" font-family="Georgia, serif" font-size="10" fill="#3D2E20">The cheapest store has the strongest consistency: writes are readable immediately.</text>
  <text x="60" y="458" font-family="Georgia, serif" font-size="10" fill="#3D2E20">The search-optimised store is 10 seconds behind on NextGen, and 60 seconds behind on Classic.</text>
  <text x="60" y="478" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#8F4A32">&#8220;Searchable the moment it is uploaded&#8221; therefore points at the cheap tier, not the fast one.</text>

  <text x="44" y="524" font-family="Georgia, serif" font-size="10" fill="#6B6560">Watch the pagination split: top-K accepts up to 10,000 results, but a response page carries at most 100.</text>
  <text x="44" y="543" font-family="Georgia, serif" font-size="10" fill="#6B6560">A client written against the first number and not the second answers confidently from a tenth of what it asked for.</text>
  <text x="44" y="566" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#5F6E38">Decide on peak queries per second, not corpus size. If that number is unknown, the cheap tier is the reversible choice.</text>
</svg>
