<svg viewBox="0 0 860 620" style="width:100%;max-width:860px;" role="img" xmlns="http://www.w3.org/2000/svg" aria-label="Diagram: where Amazon Bedrock Guardrails evaluates, and the three documented places it does not. A request flows left to right. First the input pass: the input is evaluated against every configured policy, in parallel for improved latency, and if that evaluation results in an intervention a configured blocked message is returned and the foundation model inference is discarded entirely, so no model charges are incurred. If the input passes, the foundation model runs, and then the output pass evaluates the model response, where an intervention either replaces the response with pre-configured blocked messaging or masks the sensitive information, depending on policy configuration. The policies available are content filters covering Hate, Insults, Sexual, Violence, Misconduct and Prompt Attack with configurable strength per category, denied topics, word filters matching exact custom words and phrases, sensitive information filters that block or mask entities such as social security numbers, dates of birth and addresses and support custom regular expressions, contextual grounding checks that detect responses not grounded in the source, and automated reasoning checks that validate responses against logical rules. Three gaps are marked. First, reasoning content blocks are excluded from evaluation by documentation. Second, content outside input tags is not processed by guardrails, which in a retrieval-augmented prompt means the retrieved documents where indirect prompt injection lives, since the documented example treats search results as trusted sources. Third, prompt attack filters require input tags to be present, so omitting tags, which otherwise causes the complete prompt to be evaluated, silently disables the one filter aimed at injection. A further panel records the tag boundary itself as an attack surface: a static tag suffix lets a malicious user close the XML tag and append content after the closure, which is why AWS recommends a new random suffix on every request, limited to alphanumeric characters between 1 and 20 in length, with nesting not allowed. A closing panel records the cost asymmetry, that blocking on input avoids all model charges while blocking on output pays for the guardrail on both passes plus the inference and the response already generated.">
  <title>Two passes, six policies, and three documented places the filter does not look</title>
  <rect width="860" height="620" fill="#F8F7F5"/>

  <text x="44" y="36" font-family="Georgia, serif" font-size="15" font-weight="bold" fill="#1D2322">A guardrail is a boundary, not a switch &#8212; and one edge is drawn inside the prompt</text>
  <text x="44" y="57" font-family="Georgia, serif" font-size="11" fill="#6B6560">Every gap below is documented. None of it needs a vulnerability report, only reading the page.</text>

  <!-- input pass -->
  <rect x="44" y="84" width="232" height="120" rx="3" fill="#5B7B9A" opacity="0.14"/>
  <rect x="44" y="84" width="232" height="120" rx="3" fill="none" stroke="#5B7B9A" stroke-width="1.6"/>
  <text x="58" y="106" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#3F5A73">Pass 1 &#8212; the input</text>
  <text x="58" y="126" font-family="Georgia, serif" font-size="9" fill="#2E4256">evaluated against every policy,</text>
  <text x="58" y="140" font-family="Georgia, serif" font-size="9" fill="#2E4256">in parallel, for improved latency</text>
  <rect x="58" y="152" width="204" height="40" rx="3" fill="#8A9A5B" opacity="0.20"/>
  <text x="70" y="169" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#3D4A22">intervention &#8594; blocked message,</text>
  <text x="70" y="183" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#3D4A22">and the inference is discarded</text>

  <text x="286" y="150" font-family="Georgia, serif" font-size="15" fill="#8A6A44">&#8594;</text>

  <!-- model -->
  <rect x="310" y="112" width="112" height="64" rx="3" fill="#EDE8E2"/>
  <rect x="310" y="112" width="112" height="64" rx="3" fill="none" stroke="#C9C3BC" stroke-width="1.3"/>
  <text x="330" y="140" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#3D2E20">foundation</text>
  <text x="344" y="156" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#3D2E20">model</text>

  <text x="432" y="150" font-family="Georgia, serif" font-size="15" fill="#8A6A44">&#8594;</text>

  <!-- output pass -->
  <rect x="456" y="84" width="360" height="120" rx="3" fill="#5B7B9A" opacity="0.14"/>
  <rect x="456" y="84" width="360" height="120" rx="3" fill="none" stroke="#5B7B9A" stroke-width="1.6"/>
  <text x="470" y="106" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#3F5A73">Pass 2 &#8212; the model response</text>
  <text x="470" y="126" font-family="Georgia, serif" font-size="9" fill="#2E4256">intervention is not always a block. Per policy configuration:</text>
  <rect x="470" y="138" width="166" height="54" rx="3" fill="#B5654A" opacity="0.14"/>
  <text x="482" y="156" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#8F4A32">blocked messaging</text>
  <text x="482" y="172" font-family="Georgia, serif" font-size="9" fill="#8F4A32">the response is replaced</text>
  <rect x="646" y="138" width="156" height="54" rx="3" fill="#8A9A5B" opacity="0.20"/>
  <text x="658" y="156" font-family="Georgia, serif" font-size="9" font-weight="bold" fill="#3D4A22">masking</text>
  <text x="658" y="172" font-family="Georgia, serif" font-size="9" fill="#3D4A22">the PII is redacted, the</text>
  <text x="658" y="186" font-family="Georgia, serif" font-size="9" fill="#3D4A22">answer survives</text>

  <!-- policies -->
  <text x="44" y="232" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#5F6E38">The six policy types</text>
  <rect x="44" y="242" width="772" height="52" rx="3" fill="#8A9A5B" opacity="0.10"/>
  <text x="58" y="262" font-family="Georgia, serif" font-size="9.5" fill="#3D4A22">content filters (Hate, Insults, Sexual, Violence, Misconduct, Prompt Attack &#8212; strength configurable per category) &#183; denied topics</text>
  <text x="58" y="280" font-family="Georgia, serif" font-size="9.5" fill="#3D4A22">word filters (exact match) &#183; sensitive information (block or mask; probabilistic entities + custom regex) &#183; contextual grounding &#183; automated reasoning</text>

  <!-- the three gaps -->
  <rect x="44" y="310" width="772" height="150" rx="3" fill="#B5654A" opacity="0.10"/>
  <rect x="44" y="310" width="772" height="150" rx="3" fill="none" stroke="#B5654A" stroke-width="1.8"/>
  <text x="60" y="332" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#8F4A32">Three documented places the evaluation does not reach</text>

  <text x="60" y="356" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#3D2E20">1. Reasoning content blocks.</text>
  <text x="228" y="356" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#3D2E20">Scope is &#8220;user inputs or model responses (excluding reasoning content blocks)&#8221;.</text>

  <text x="60" y="382" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#3D2E20">2. Anything outside input tags.</text>
  <text x="248" y="382" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#3D2E20">&#8220;Content outside of input tags isn&#8217;t processed by guardrails.&#8221;</text>
  <text x="76" y="398" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">In a RAG prompt that is the retrieved documents &#8212; the docs call them &#8220;trusted sources&#8221;; injection arrives there.</text>

  <text x="60" y="424" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#3D2E20">3. Prompt Attack, when you do not tag.</text>
  <text x="296" y="424" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#3D2E20">It &#8220;require[s] input tags to be present&#8221;.</text>
  <text x="76" y="440" font-family="Georgia, serif" font-size="9.5" font-weight="bold" fill="#8F4A32">No tags means the whole prompt is evaluated &#8212; except by the one filter aimed at injection. It reads as configured.</text>

  <!-- the boundary is text -->
  <rect x="44" y="476" width="772" height="74" rx="3" fill="#C4A484" opacity="0.18"/>
  <text x="60" y="496" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#8A6A44">And the boundary itself is text the attacker can see</text>
  <text x="60" y="516" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#3D2E20">&#8220;A static tag can result in a malicious user closing the XML tag and appending malicious content after the tag closure.&#8221;</text>
  <text x="60" y="536" font-family="Georgia, serif" font-size="10" fill="#3D2E20">Hence: a new random tagSuffix every request &#183; alphanumeric, 1 to 20 characters &#183; nesting not allowed.</text>

  <text x="44" y="576" font-family="Georgia, serif" font-size="10" fill="#6B6560">Cost asymmetry: an input block pays for the guardrail only &#8212; no model charges. An output block pays both passes, the inference, and the response nobody sees.</text>
  <text x="44" y="598" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#5F6E38">The filter models update automatically, and the console cannot test selective evaluation. Validate through the SDK, on a schedule.</text>
</svg>
