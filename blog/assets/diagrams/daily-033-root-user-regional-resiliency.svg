<svg viewBox="0 0 860 520" style="width:100%;max-width:860px;" role="img" xmlns="http://www.w3.org/2000/svg" aria-label="Diagram: where an AWS ConsoleLogin event is recorded, and how root user sign-in resiliency changes it. The upper half shows the change: root user sign-in is now served across US East N. Virginia, US East Ohio and US West Oregon, with traffic distributed across all three Regions automatically and no customer configuration, so the resulting ConsoleLogin event is recorded in us-east-1, us-east-2 or us-west-2 depending on which Region processed the request. The lower half shows the detection consequence: a security baseline whose CloudTrail metric filter, alarm or EventBridge rule is scoped only to us-east-1 will see root sign-ins processed there and will silently miss those processed in Ohio or Oregon, and because a missing event produces no error the alarm staying quiet is indistinguishable from nobody using root. A side panel records that the Region recorded in a ConsoleLogin event already varied by user type and endpoint before this change: an IAM user on the global endpoint with an account alias cookie present is recorded in us-east-2, eu-north-1 or ap-southeast-2 based on latency from the sign-in location, an IAM user on the global endpoint without that cookie is recorded in us-east-1, and an IAM user on a regional endpoint is recorded in the Region of that endpoint. A closing note records the durable fix: an organisation trail applied to all Regions captures the event wherever it lands, which is why the control belongs at the trail rather than at a Region-scoped alarm.">
  <title>The sign-in moved to three Regions and took its audit event with it</title>
  <rect width="860" height="520" fill="#F8F7F5"/>

  <text x="44" y="36" font-family="Georgia, serif" font-size="15" font-weight="bold" fill="#1D2322">The signal moved with the service</text>
  <text x="44" y="57" font-family="Georgia, serif" font-size="11" fill="#6B6560">Root sign-in is now served from three Regions. The ConsoleLogin event is recorded wherever the sign-in was processed.</text>

  <!-- root sign-in -->
  <rect x="44" y="84" width="180" height="76" rx="3" fill="#8A9A5B" opacity="0.14"/>
  <rect x="44" y="84" width="180" height="76" rx="3" fill="none" stroke="#8A9A5B" stroke-width="1.5"/>
  <text x="58" y="106" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#5F6E38">Root user signs in</text>
  <text x="58" y="125" font-family="Georgia, serif" font-size="9" fill="#3D4A22">Routed automatically.</text>
  <text x="58" y="138" font-family="Georgia, serif" font-size="9" fill="#3D4A22">No Region to select,</text>
  <text x="58" y="151" font-family="Georgia, serif" font-size="9" fill="#3D4A22">no change to sign-in.</text>

  <text x="233" y="128" font-family="Georgia, serif" font-size="15" fill="#8A6A44">&#8594;</text>

  <!-- three regions -->
  <rect x="254" y="84" width="270" height="76" rx="3" fill="#5B7B9A" opacity="0.13"/>
  <rect x="254" y="84" width="270" height="76" rx="3" fill="none" stroke="#5B7B9A" stroke-width="1.5"/>
  <text x="268" y="106" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#3F5A73">Served across three Regions</text>
  <text x="268" y="126" font-family="Consolas, monospace" font-size="9" fill="#2E4256">us-east-1   N. Virginia</text>
  <text x="268" y="139" font-family="Consolas, monospace" font-size="9" fill="#2E4256">us-east-2   Ohio</text>
  <text x="268" y="152" font-family="Consolas, monospace" font-size="9" fill="#2E4256">us-west-2   Oregon</text>

  <text x="533" y="128" font-family="Georgia, serif" font-size="15" fill="#8A6A44">&#8594;</text>

  <!-- event lands -->
  <rect x="554" y="84" width="262" height="76" rx="3" fill="#C4A484" opacity="0.20"/>
  <rect x="554" y="84" width="262" height="76" rx="3" fill="none" stroke="#C4A484" stroke-width="1.5"/>
  <text x="568" y="106" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#8A6A44">ConsoleLogin recorded</text>
  <text x="568" y="126" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">In the Region that processed</text>
  <text x="568" y="139" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">the request &#8212; which one is</text>
  <text x="568" y="152" font-family="Georgia, serif" font-size="9.5" font-weight="bold" fill="#8A6A44">not yours to choose.</text>

  <!-- the gap -->
  <rect x="44" y="184" width="480" height="150" rx="3" fill="#B5654A" opacity="0.10"/>
  <rect x="44" y="184" width="480" height="150" rx="3" fill="none" stroke="#B5654A" stroke-width="1.8"/>
  <text x="58" y="207" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#8F4A32">What a Region-scoped alarm now sees</text>

  <rect x="58" y="220" width="200" height="46" rx="3" fill="#8A9A5B" opacity="0.18"/>
  <text x="70" y="239" font-family="Consolas, monospace" font-size="9" fill="#3D4A22">us-east-1 sign-in</text>
  <text x="70" y="255" font-family="Georgia, serif" font-size="9.5" font-weight="bold" fill="#5F6E38">Alarm fires. Seen.</text>

  <rect x="272" y="220" width="238" height="46" rx="3" fill="#B5654A" opacity="0.18"/>
  <text x="284" y="239" font-family="Consolas, monospace" font-size="9" fill="#3D2E20">us-east-2 / us-west-2 sign-in</text>
  <text x="284" y="255" font-family="Georgia, serif" font-size="9.5" font-weight="bold" fill="#8F4A32">Nothing. Never seen.</text>

  <text x="58" y="288" font-family="Georgia, serif" font-size="9.5" fill="#3D2E20">The rule does not fail. It does not error. It stays quiet &#8212;</text>
  <text x="58" y="303" font-family="Georgia, serif" font-size="9.5" font-weight="bold" fill="#8F4A32">and a quiet root alarm is what a healthy account looks like.</text>
  <text x="58" y="323" font-family="Georgia, serif" font-size="9" font-style="italic" fill="#6B6560">Nothing in the account distinguishes the two.</text>

  <!-- prior variation -->
  <rect x="544" y="184" width="272" height="150" rx="3" fill="#EDE8E2"/>
  <rect x="544" y="184" width="272" height="150" rx="3" fill="none" stroke="#C9C3BC" stroke-width="1.2"/>
  <text x="558" y="207" font-family="Georgia, serif" font-size="10.5" font-weight="bold" fill="#3D2E20">This already varied for IAM users</text>
  <text x="558" y="227" font-family="Georgia, serif" font-size="9" fill="#6B6560">Global endpoint, alias cookie present:</text>
  <text x="558" y="240" font-family="Consolas, monospace" font-size="8.5" fill="#3D2E20">us-east-2 / eu-north-1 / ap-southeast-2</text>
  <text x="558" y="252" font-family="Georgia, serif" font-size="8.5" font-style="italic" fill="#6B6560">chosen by latency from the sign-in location</text>
  <text x="558" y="274" font-family="Georgia, serif" font-size="9" fill="#6B6560">Global endpoint, no cookie:</text>
  <text x="558" y="287" font-family="Consolas, monospace" font-size="8.5" fill="#3D2E20">us-east-1</text>
  <text x="558" y="309" font-family="Georgia, serif" font-size="9" fill="#6B6560">Regional endpoint:</text>
  <text x="558" y="322" font-family="Consolas, monospace" font-size="8.5" fill="#3D2E20">the Region of that endpoint</text>

  <!-- fix -->
  <rect x="44" y="354" width="772" height="80" rx="3" fill="#8A9A5B" opacity="0.12"/>
  <rect x="44" y="354" width="772" height="80" rx="3" fill="none" stroke="#8A9A5B" stroke-width="1.8"/>
  <text x="60" y="377" font-family="Georgia, serif" font-size="11" font-weight="bold" fill="#5F6E38">The fix that does not need repeating</text>
  <text x="60" y="397" font-family="Georgia, serif" font-size="10" fill="#3D4A22">Collect with an organisation trail applied to all Regions, so the event is captured wherever it lands. Then alert off the trail.</text>
  <text x="60" y="417" font-family="Georgia, serif" font-size="10" font-weight="bold" fill="#5F6E38">Three Region-scoped alarms is the same bug, deferred to the next time AWS changes routing.</text>

  <text x="44" y="462" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#6B6560">AWS states the action in the announcement: extend monitoring and alerting to cover N. Virginia, Ohio and Oregon.</text>
  <text x="44" y="480" font-family="Georgia, serif" font-size="10" font-style="italic" fill="#6B6560">Nothing in the account will tell you that you skipped it. &#8220;Global&#8221; is a routing decision, not a fact you can build detection on.</text>
</svg>
